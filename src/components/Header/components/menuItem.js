import styled from 'styled-components';

export default styled.li`
    display: block;
    margin: 15px;
    cursor: pointer;
    transition: all 0.3s ease-in;
    &:hover {
        color: rebeccapurple;
        cursor: pointer;
    }
`;