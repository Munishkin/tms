import styled from 'styled-components';

export default styled.ul`
    font-family: 'Poppins',sans-serif;
    position: relative;
    display: flex;
    flex-direction: row;
    list-style: none;
    font-size: 20px;
    font-weight: 500;
    color: slategray;
`;